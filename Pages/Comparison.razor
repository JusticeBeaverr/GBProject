@inject ViewModels.Smartphone.ISmartphoneViewModel viewModel
@using GadgetBlitzPZ.Models.Smartphone
@using GadgetBlitzPZ.Pages.Templates
@using System.Linq
@using Blazorise.Icons.FontAwesome
@using Microsoft.JSInterop

@page "/comparison"

<Style></Style>

<h3>Porównanie Smartfonów</h3>

<div class="table-container custom-shadow">
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th></th>
                @foreach (var smartphone in viewModel.SmartphonesComparisonListComparsionView)
                {
                    <th>
                        <div class="d-flex flex-column align-items-center">
                            @if (smartphone.Urls != null && smartphone.Urls.Any())
                            {
                                <img src="@smartphone.Urls.First()" alt="Smartphone Image" style="width: 160px; height: 200px; object-fit: cover; object-position: center; border-radius: 3%; transform: scale(0.9);" />
                            }
                            <h5>@smartphone.Name</h5>
                        </div>
                    </th>
                }
            </tr>
        </thead>
        <tbody>
            <tr>
                <th>Marka</th>
                @foreach (var smartphone in viewModel.SmartphonesComparisonListComparsionView)
                {
                        <td>@(smartphone.Data.General?.Brand ?? "---")</td>
                }
        </tr        >
    <tr>
        <th>Rozmiar</th>
        @foreach (var smartphone in viewModel.SmartphonesComparisonListComparsionView)
        {
            <td>@(smartphone.Data.Display.Size__inch.HasValue ? smartphone.Data.Display.Size__inch.Value.ToString() : "---")</td>
        }
    </tr>
    <tr>
        <th>Bateria</th>
        @foreach (var smartphone in viewModel.SmartphonesComparisonListComparsionView)
        {
            <td>@(smartphone.Data.Battery.Capacity__mAh.HasValue ? smartphone.Data.Battery.Capacity__mAh.Value : "---")</td>
        }
    </tr>
    <tr>
        <th>System</th>
        @foreach (var smartphone in viewModel.SmartphonesComparisonListComparsionView)
        {
            <td>@(smartphone.Data.Software?.Os ?? "---")</td>
        }
    </tr>
    <tr>
        <th>Pamięć</th>
        @foreach (var smartphone in viewModel.SmartphonesComparisonListComparsionView)
        {
            <td>@(smartphone.Data.Storage.Capacity__gb.HasValue ? smartphone.Data.Storage.Capacity__gb.Value.ToString() : "---")</td>
        }
    </tr>
    <tr>
        <th>Cena</th>
        @foreach (var smartphone in viewModel.SmartphonesComparisonListComparsionView)
        {
            <td>@(smartphone.Price.HasValue ? smartphone.Price.Value.ToString() : "---")</td>
        }
    </tr>
    <tr>
        <th>Aparat główny (MP)</th>
        @foreach (var smartphone in viewModel.SmartphonesComparisonListComparsionView)
        {
            <td>@(smartphone.Data.Camera.Camera_back__mp.HasValue ? smartphone.Data.Camera.Camera_back__mp.Value.ToString() : "---")</td>
        }
    </tr>
    <tr>
        <th>Procesor</th>
        @foreach (var smartphone in viewModel.SmartphonesComparisonListComparsionView)
        {
            <td>@(smartphone.Data.Cpu?.Type ?? "---")</td>
        }
    </tr>
    <tr>
        <th>Rdzenie</th>
        @foreach (var smartphone in viewModel.SmartphonesComparisonListComparsionView)
        {
            <td>@(smartphone.Data.Cpu?.Number_of_cores.ToString() ?? "---")</td>
        }
    </tr>
    <tr>
        <th>Typ wyświetlacza</th>
        @foreach (var smartphone in viewModel.SmartphonesComparisonListComparsionView)
        {
            <td>@(smartphone.Data.Display?.Type ?? "---")</td>
        }
    </tr>
    <tr>
        <th>Kolor</th>
        @foreach (var smartphone in viewModel.SmartphonesComparisonListComparsionView)
        {
            <td>@(smartphone.Data.Design?.Color ?? "---")</td>
        }
    </tr>
    <tr>
        <th>Waga</th>
        @foreach (var smartphone in viewModel.SmartphonesComparisonListComparsionView)
        {
            <td>@(smartphone.Data.Measurements.Weight__g.HasValue ? smartphone.Data.Measurements.Weight__g.Value.ToString() : "---")</td>
        }
    </tr>
            <tr>
                <th>MagSafe</th>
                @foreach (var smartphone in viewModel.SmartphonesComparisonListComparsionView)
                {
                    <td>@(smartphone.Data.Has_magsafe && smartphone.Data.Has_magsafe? "Tak" : "Nie")</td>
                }
            </tr>
            <tr>
                <th>Wodoodporność</th>
                @foreach (var smartphone in viewModel.SmartphonesComparisonListComparsionView)
                {
                    <td>@(smartphone.Data.Is_water_resistant.HasValue && smartphone.Data.Is_water_resistant.Value ? "Tak" : "Nie")</td>
                }
            </tr>
            <tr>
                <th>Szybkie ładowanie</th>
                @foreach (var smartphone in viewModel.SmartphonesComparisonListComparsionView)
                {
                    <td>@(smartphone.Data.Has_fast_charge.HasValue && smartphone.Data.Has_fast_charge.Value ? "Tak" : "Nie")</td>
                }
            </tr>
            <tr>
                <th>Ładowanie bezprzewodowe</th>
                @foreach (var smartphone in viewModel.SmartphonesComparisonListComparsionView)
                {
                    <td>@(smartphone.Data.Has_wireless_charging.HasValue && smartphone.Data.Has_wireless_charging.Value ? "Tak" : "Nie")</td>
                }
            </tr>
            <tr>
                <th>Odporność na wstrząsy</th>
                @foreach (var smartphone in viewModel.SmartphonesComparisonListComparsionView)
                {
                    <td>@(smartphone.Data.Is_shock_resistant.HasValue && smartphone.Data.Is_shock_resistant.Value ? "Tak" : "Nie")</td>
                }
            </tr>
            <tr>
                <th>Wibracyjny budzik</th>
                @foreach (var smartphone in viewModel.SmartphonesComparisonListComparsionView)
                {
                    <td>@(smartphone.Data.Has_vibrating_alarm.HasValue && smartphone.Data.Has_vibrating_alarm.Value ? "Tak" : "Nie")</td>
                }
            </tr>
            <tr>
                <th>Rozpoznawanie twarzy</th>
                @foreach (var smartphone in viewModel.SmartphonesComparisonListComparsionView)
                {
                    <td>@(smartphone.Data.Has_face_recognition.HasValue && smartphone.Data.Has_face_recognition.Value ? "Tak" : "Nie")</td>
                }
            </tr>
            <tr>
                <th>Odcisk palca</th>
                @foreach (var smartphone in viewModel.SmartphonesComparisonListComparsionView)
                {
                    <td>@(smartphone.Data.Has_fingerprint_reader.HasValue && smartphone.Data.Has_fingerprint_reader.Value ? "Tak" : "Nie")</td>
                }
            </tr>
            <tr>
                <th>Gniazdo słuchawkowe</th>
                @foreach (var smartphone in viewModel.SmartphonesComparisonListComparsionView)
                {
                    <td>@(smartphone.Data.Has_headphone_jack.HasValue && smartphone.Data.Has_headphone_jack.Value ? "Tak" : "Nie")</td>
                }
            </tr>
</tbody>


    </table>
</div>


@code {

}
